# 环境基线

> 本文件由 Phase 0 · Step 0-1 自动生成，记录改造前的环境状态作为基线。
> 生成时机：venv 新建并执行 `pip install -r requirements.txt` 完成后。

---

## 1. Python 版本

```
Python 3.14.5
Full version: 3.14.5 (tags/v3.14.5:5607950, May 10 2026, 10:43:50) [MSC v.1944 64 bit (AMD64)]
```

## 2. 虚拟环境

- 路径：`e:\PrivateProject\chanlun\venv`
- 创建命令：`python -m venv venv`
- 解释器：`e:\PrivateProject\chanlun\venv\Scripts\python.exe`

## 3. pip freeze 输出（依赖清单）

```
altair==6.2.2
anyio==4.14.2
attrs==26.1.0
baostock==0.9.3
blinker==1.9.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.4.2
colorama==0.4.6
contourpy==1.3.3
cryptography==50.0.0
cycler==0.12.1
fonttools==4.63.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.25.2
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kiwisolver==1.5.0
MarkupSafe==3.0.3
matplotlib==3.11.1
mootdx==0.11.7
mpld3==0.5.12
narwhals==2.24.0
numpy==2.5.2
packaging==26.3
pandas==3.0.5
pillow==12.3.0
plotly==6.9.0
prettytable==3.18.0
protobuf==7.35.1
py-mini-racer==0.6.0
pyarrow==25.0.1
pycparser==3.0
pydeck==0.9.3
pyparsing==3.3.2
pytdx==1.72
python-dateutil==2.9.0.post0
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
sniffio==1.3.1
starlette==1.6.0
streamlit==1.62.0
tdxpy==0.2.7
tenacity==8.5.0
toml==0.10.2
tqdm==4.70.0
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
uvicorn==0.52.4
watchdog==6.0.0
wcwidth==0.8.2
websockets==16.1.1
```

## 4. 关键依赖二次确认（对应 Step 0-4 增强 A）

> 用于确认 `enhanced_viz.py` 的 matplotlib+mpld3 依赖、`plotly_viz.py` 的 plotly 依赖、`web/app.py` 的 streamlit 依赖、数据层的 baostock/mootdx/pytdx 依赖均存在。

| 依赖 | 用途 | 已安装版本 | 状态 |
|---|---|---|---|
| matplotlib | enhanced_visualizer.py 绘图 | 3.11.1 | ✅ |
| mpld3 | enhanced_visualizer.py HTML 转换 | 0.5.12 | ✅ |
| plotly | plotly_visualizer.py 交互图 | 6.9.0 | ✅ |
| streamlit | web/app.py Web 应用 | 1.62.0 | ✅ |
| requests | tushare_client.py HTTP 请求（Tushare 私有代理） | 2.34.2 | ✅ |
| pandas | 通用数据处理 / 样本列映射 | 3.0.5 | ✅ |
| numpy | 通用数值计算 | 2.5.2 | ✅ |
| baostock | ⚠️ **已安装但本次改造弃用**（baostock 当前环境无法取 ETF 行情） | 0.9.3 | ⛔ 弃用 |
| mootdx | ⚠️ **已安装但本次改造弃用**（mootdx 历史日期范围查询 bug + 服务器权限/连通性不稳） | 0.11.7 | ⛔ 弃用 |
| pytdx | mootdx 底层依赖，随 mootdx 一并弃用 | 1.72 | ⛔ 弃用 |

> **⚠️ 数据源决策（v4，详见《分步执行清单》"数据源决策"节）**：本次改造全局统一使用 **Tushare** 取数（经私有代理 `https://ts-2.cwy666.com`，已开通 `daily`/`index_daily`/`fund_daily`），由项目根 `tushare_client.py` 封装。baostock 与 mootdx **不再作为任何样本或改造路径的数据源**，但其包仍保留在环境中（仅弃用、未卸载），以便 Phase 7 回退或历史参考。`tushare` 包本身未列入 `requirements.txt`（当前 `tushare_client.py` 直接基于 `requests` 调用代理，不依赖官方 `tushare` SDK）。

## 5. 安装命令记录

```powershell
cd e:\PrivateProject\chanlun
python -m venv venv
.\venv\Scripts\python.exe -m pip install --upgrade pip
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

> 依赖全部安装成功，无缺失包。

## 6. 通过标准确认

- ✅ Python 版本可用（3.14.5，64 位）
- ✅ `pip install -r requirements.txt` 全部依赖安装成功，无缺失
- ✅ 环境基线文件已生成（本文件）
- ✅ 关键依赖二次确认（matplotlib、mpld3、plotly、streamlit、requests、pandas、numpy）均存在
- ✅ 数据源决策确认：全局使用 Tushare（私有代理 ts-2.cwy666.com，daily/index_daily/fund_daily 已开通）；baostock/mootdx 标记为弃用（包仍安装但未使用）
