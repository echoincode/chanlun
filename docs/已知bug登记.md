# 已知 Bug 登记

> **维护人**：缠论项目工程优化（v4 改造）
> **更新时间**：2026-08-20
> **说明**：本文档登记项目已知 bug 及其处理状态。v4 改造后数据源已统一为 Tushare，原 mootdx/baostock 数据源已弃用，相关 bug 标注为"随弃用作废"。

---

## 已随数据源弃用作废的 Bug

### Bug #1: mootdx 港股备用服务器 IP 拼写错误

| 项 | 内容 |
|---|---|
| 文件 | `mootdx_data_fetcher.py` 第 832 行 |
| 问题描述 | 港股备用服务器 IP `1124.71.66.200` 疑似笔误，正确 IP 应为 `124.71.66.200` |
| 影响 | 港股取数可能失败或超时 |
| v4 处理 | **随 mootdx 弃用作废**。mootdx 已弃用，不迁移不修复。原文件保留在 `project_backup/` 冻结，bug 不再影响改造区代码 |
| 状态 | ❌ 已作废 |

### Bug #2: mootdx get_k_data 历史日期范围查询 Bug

| 项 | 内容 |
|---|---|
| 文件 | `mootdx_data_fetcher.py`（get_k_data 方法） |
| 问题描述 | mootdx 0.11.7 库在查询较早历史日期范围时返回空数据；部分行情服务器存在权限/连通性问题 |
| 影响 | A股/ETF 历史数据可能获取失败 |
| v4 处理 | **随 mootdx 弃用作废**。数据源统一为 Tushare 后不再依赖 mootdx，该 bug 不再影响新代码 |
| 状态 | ❌ 已作废 |

### Bug #3: baostock 无法获取 ETF 行情

| 项 | 内容 |
|---|---|
| 文件 | `baostock_data_fetcher.py` |
| 问题描述 | baostock 数据源不支持 ETF 行情获取，调用 ETF 接口返回空或报错 |
| 影响 | ETF 分析功能完全不可用 |
| v4 处理 | **随 baostock 弃用作废**。baostock 已弃用，Tushare 支持 ETF 日线（经 `fund_daily` 接口） |
| 状态 | ❌ 已作废 |

---

## v4 新代码的已知限制

### 限制 #1: Tushare 仅支持日线

| 项 | 内容 |
|---|---|
| 位置 | `src/data/tushare_fetcher.py`、`src/cli/runner.py` |
| 问题描述 | Tushare 数据源当前仅开通日线权限，分钟线接口未开通 |
| 影响 | 分钟线分析功能不可用，UI 选择分钟线时主动提示并中断 |
| v4 处理 | 前端（web/app.py 和 CLI）均已添加分钟线提示，不静默失败。待 Tushare 开通分钟线后可扩展 |
| 状态 | ⚠️ 已知限制，待后续扩展 |

### 限制 #2: Tushare 不支持港股

| 项 | 内容 |
|---|---|
| 位置 | `src/cli/runner.py`（fetch_data 函数） |
| 问题描述 | Tushare 需单独权限才能获取港股数据，当前未开通 |
| 影响 | 港股分析不可用，输入港股代码时主动提示并中断 |
| v4 处理 | runner 层对 hk 类型直接抛 ValueError，web 层和 CLI 捕获后友好提示。待开通港股权限后可扩展 |
| 状态 | ⚠️ 已知限制，待后续扩展 |

---

## 可视化修复记录

### 修复 #1: 笔绘制不连续（v4.1 修复）

| 项 | 内容 |
|---|---|
| 文件 | `src/visual/plotly_viz.py` `_draw_segments` 方法 |
| 问题描述 | 原实现为每个笔单独创建独立线段（N 笔 = N 条互不相连的线），导致视觉上"笔在乱画、不连续" |
| 修复方案 | 将所有笔端点按时间顺序收集，连成一条连续折线（1 条 trace 包含全部端点） |
| 修复时间 | 2026-08-20 |
| 验证结果 | 600519.SH 7 端点连成 1 条连续折线、510300.SH 3 端点连成 1 条连续折线，无相邻重复坐标 |
| 状态 | ✅ 已修复 |

---

## 总结

- **mootdx/baostock 遗留 bug**：共 3 项，均随数据源弃用作废，不影响 v4 新代码
- **v4 已知限制**：2 项（仅日线、不含港股），属 Tushare 权限范围限制，待后续扩展
- **可视化修复**：1 项（笔连续折线），已修复并验证通过
